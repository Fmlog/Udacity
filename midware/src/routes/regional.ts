import express from "express";
import logger from "../logger";

const regional = express.Router();

// regional.get("/", logger, function (req, res) {
//   res.status(200).send("Regional block");
// });

export default regional;
