import express from "express";
import logger from "../logger"

const continent = express.Router();

// continent.get("/", logger, (req, res) => {
//   res.status(200).send("Continent endpoint");
// });

export default continent;
